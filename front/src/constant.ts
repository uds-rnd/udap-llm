// for CSS
const LOGOHEIGHT: number = 70;
const MH: number = Math.round(window.screen.height / 100);
const HEADERHEIGHT: number = MH * 2.5;
const SIDEBARWIDTH: number = 240;
const SCROLLBARWIDTH = 30;

export const CONST = {
  LOGOHEIGHT,
  CONTENTMARGINTOP: HEADERHEIGHT + MH,
  HEADERHEIGHT,
  MH,
  SIDEBARWIDTH,
  SCROLLBARWIDTH,
};
